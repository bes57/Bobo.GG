.venv/
__pycache__/
*.pyc
*.pyo
.env
.DS_Store
.claude/worktrees/
# Derived cache: recent-records precompute, regenerated on demand + keyed on
# data mtimes (which differ per checkout, so committing it gives no benefit).
data/recent_records.json
testing_lab/v8/data/vctmm_live.db
# Per-host retry ledger for match stats re-scrapes (MatchStatsIntegrity).
# Attempt counts/timestamps are local runtime state, not shared data.
data/stats_recheck.json
