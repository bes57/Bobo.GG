# v8 Lab — power, mechanisms & the adversarial audit

Repo mirror of `/testing/report/v8_lab` (generated by `testing_lab/gen_v8_report.py`;
every number sourced from `testing_lab/v8/stats/*.json`). One-day sole-researcher
program, 2026-07-28, on the sha-verified expanded frame (holdout n=1217, +335
corpus additions). **The verdicts below are as amended by the independent
adversary** (`adversary_report.md`, published verbatim in §8 of the page).

> **THE HOLDOUT IS SPENT.** 398 holdout numbers are recorded on disk (163
> primary candidate looks, 227 sweep checkpoints, 8 iteration diagnostics —
> `compose_looks.json`), including per-grid-point holdout LLs for entire
> sweeps. This wave's selections were verifiably train-only, but any future
> "train-only" claim on this frame is unfalsifiable with those menus
> published. Anything new on 2025-26 is exploratory by definition;
> confirmatory power now requires fresh series (§10).

## §0 Verdict — adversary-amended, and final

1. **v6 stands; no stack cleared the gate.** All three preregistered compose
   stacks scored HOLD (S1 +1.958m, S2 −0.140m, S3 −7.874m; `compose_gate.json`
   — G1/G2/G3 failed on every stack), and the adversary reproduced all three
   under his own engine and bootstraps. Co-signed, not self-graded.
2. **CORRECTION over Phase 0's prose.** "0/23 v7 configs distinguishable" is
   wrong by the program's own rules: under the program-standard cross floor
   (5.889m) and the program's own block bootstraps, **sym_6 (−10.32m), sym_8
   (−6.24m), surprise_12_20 (−7.37m) and boxexp_c3 (−6.00m) WERE
   distinguishable — as losses** (adversary F1, CONFIRMED-FLAW; Phase 0 had
   used the laxer per-pair MDE₈₀ standard). The near-ties — sym_20 (−1.65m)
   and consist_16_10 (−0.39m) — remain unresolved: those rejections were coin
   flips. The §1 ladder is re-plotted accordingly (`page_v7_ladder.json`).
3. **Demoted by adversarial review** (presented as noise-floor leads, not
   wins): 3e stand-in shrink's EWC bucket (+3.46m at n=291 — inside its 3.63m
   floor, flips to −2.96m under drop-top-5%, dummy beats it on the legacy
   bucket); event-class fade's "positive in all subpops" (subpops overlap up
   to 97.7% of the holdout — five signs, one observation; negative on its own
   EWC target rows); 5d's 7.4-vs-24.8-game half-life contrast (profile CIs
   overlap enormously, DL pooling τ²=0); compose S1's gated gain (non-gated
   delta exactly 0.000, gated +13.39m inside a ~15m bucket floor, overall
   flips to −3.96m under drop-top-5%).
4. **The ONE surviving lead: H3 cold starts.** Rows where either team has
   <10 prior maps: **+71.7m at n=39** (bucket floor 32.9m; drop-top-5%
   +53.1m; event-jackknife min +66.0m) — survives every fragility attack.
   Caveats attached: 4 events supply the rows; the parent model loses overall
   (−5.32m). A lead that has earned a test on new data — not a promotion.
5. **The holdout is spent** (banner above).

Key state: v6 holdout LL **0.64216** (β 0.1152, n=1217) — production
unchanged. Instrument floors **1.773m within / 5.889m cross**. max|team bias|
14.8pp (v6); best candidate 12.6pp (1b) but mean|bias| worsens. Live week:
**−$308.69 realized** (not noise, p<0.00025; blame ≈60-70% config, ≈25% model).

## §1 Power

- MDE₈₀ at n=1007: 1.92m within / 6.38m cross; expansion buys ~8% →
  **1.773m / 5.889m at n=1217** (composition-adjusted, preregistered rule).
  A 2m cross-family effect needs n ≈ 10,552. (`power_mde*.json`,
  `page_mde_curve.json`)
- v7 ladder re-adjudication per §0.2. Directional consistency (every
  alternative pointed worse) was v7's real finding, not adjudicated magnitudes.
- Ledger reclassification: **5 REFUTED / 27 UNRESOLVED / 0 CONFIRMED** of 32
  entries (`ledger_reclass.json`). 27 verdicts were statements about noise;
  all UNRESOLVED entries are re-openable with a power annotation (§10).
- Variance reduction measured: pairing 33×/377× (in use); CUPED l_v6 1.18×
  cross (adopted in referee.py; the ≥1.5× trigger did not fire); block DEFF
  median 0.61 — iid CIs conservative at 18 events. (`variance_reduction.json`)
- Bucket floors: bo5 45.1m, elite-vs-floor 39.5m, grand final 33.0m, EWC-class
  21.4m (cross) — bucket claims at those sizes never had support.

## §2 Corpus

- +335 series / 31 events backfilled 2023-26 (holdout 1007→1217 scoreable);
  prefranchise corpus scraped separately (155 series, 6 intl LANs);
  **194 regional 2021-22 events deferred** — the big power lever, untouched.
  (`corpus_blocks.json`, `corpus_diff.json`)
- Verification: mechanical 490/490 series · 1134/1134 maps; live seeded
  re-fetch sample 278/278. **Limit (adversary F7): fetch-independent but
  parser-dependent** — a systematic selector bug would pass both layers.
- Hazards logged: `MapNum=="all"` aggregate rows double-count naive consumers;
  2024/2025_shanghai_masters + 2025_super_champions_cup will be classed intl
  by the harness substring rule at rebuild.

## §3 Context

- 3a lineup-conditioned EWC down-weight: **DEAD at train** (falsifier fired;
  NLL monotone worse toward w0=0). Blanket weight: train argmin at the 1.2
  UP-weight edge; holdout −0.05m. 3d learned class weights: **DEAD** (−5.74m,
  iid CI [−10.0,−1.2]; champions weight collapses to ~0 and anti-validates;
  fitted w_ewc 1.018, CI [0.4,3.4] ⊇ 1.0 — the "EWC games mislead" falsifier
  fired). Hand-set {playoffs 1.6, Champions 2.0} stands. (`context_weights.json`)
- 3b decomposition (the deliverable): **at HL5, v7's "form is mean-reverting"
  was scouting/footage-exposure in disguise** (b_form −0.130 → +0.012 with
  controls, 109% absorbed; HL3 58%); neither survives holdout anyway.
- 3c stakes: inside floor, direction wrong (train wants elim games
  DOWN-weighted). 3b-adjacency: untestable at n=57, sign opposite fatigue.
- 3e stand-in shrink: −0.40m overall (floor); EWC bucket +3.46m **demoted**
  per §0.3 — retained only as a mechanism note for a future frame.
- Seriousness observables are real (stand-in rate 23.3% EWC vs 6.7% VCT rows)
  — they just don't convert into probability signal. (`lineups_coverage.json`)

## §4 Decay

- Re-race with CV: consist_16_10 −0.53m, sym_20 −2.17m, sym_24 −2.42m,
  sym_16 −2.33m — **ties** (pre-committed wording: "unresolved either way at
  n=1217"). CV shrinks cross-family CIs 13-16%, not enough. (`decay_rerace.json`)
- Five new axes: all INSIDE NOISE FLOOR vs v6; patch fade **killed** (best
  train LL of any config → −5.5m holdout, falsifier fired); lineup-continuity
  real-but-redundant with year-boundary continuity; event-class fade +0.24m
  demoted per §0.3. (`decay_axes.json`)
- Performance-defined form: every definition fits mean-reverting on train and
  scores ≤ v6 on holdout; player-R2.0 overfits hardest (train +2.09m →
  holdout −5.24m). (`decay_form.json`)
- Subpop panel: no config wins any subpop at bucket MDE. (`decay_subpops.json`)

## §5 Bias mechanisms

- **H1 censoring: DEAD at the premise.** 12/5140 maps (0.23%) hit 13-0;
  density falls toward the bound; cap-ratio Q4/mid 1.00 (needed ≥2). Tobit
  −0.46m, elite bias unmoved. Round-BT −0.22m (tie); **round-level power play
  refuted**: k_eff ≈ 1.25 Fisher / 0.80 clustered (claim needed ≥7).
- **H2 connectivity: DEAD at the gate.** |bias|~centrality ρ −0.077
  [−0.42,+0.28]. Signed correlations are composition (elite travel, floor
  stays home; PRX is the most central and most under-rated). No rescue fits.
- **H3 uncertainty: loses overall, wins the cold tail.** Core −8.87m LOSS;
  calendar-noise 1b −11.75m (train mirage); 5d roster-typed −5.32m (inside
  cross floor). Cold buckets: §0.4. Half-life table demoted per §0.3
  (pooled HL ≈ 8 games; "heterogeneity unsupported at this n" is the formal
  inference). n_eff telemetry emitted for sizing (`h3_neff.json`).
- **H4 aggregation: premise inverted.** Maps are OVER-dispersed (σ̂_u 0.72
  [0.38,1.00]); v6's train-fit β already pays the correction (0.115/0.141 ≈
  the iid-equivalent rescale); both links inside floor; P(bo5) for strong
  favorites falls, not rises.
- Reliability (favorite frame): the uncertainty models buy cold-tail gains by
  shrinking toward 0.5. (`page_reliability.json`, `page_caterpillar.json`)

## §6 Buckets

All three stacks HOLD (`compose_gate.json`); nearly every bucket delta sits
inside its scaled floor (family MDE·√(N/n)). S1's huge-gap −8.86m (n=56)
fails the gate's fixed-threshold G3; S2 fails on domestic-EMEA −4.41m and
favorite-[0.7,0.8) −7.63m; S3 is an 18-bucket wipeout. Gated split S1:
+13.39m on 178 gated rows vs exactly 0.000 elsewhere. **PRX/NRG residual
(≈−10pp) persists under every stack** — named future work; not stand-in load,
not event-class recency, not rating uncertainty.

## §7 Autopsy (operator implementation memo — README rule 9; one-week sample)

−$308.69 realized (719 fills, 28 settled events; fees $0 verified maker).
Not noise (CRN p < 0.00025 vs the flat-floor edge hypothesis). Adverse
selection −8.5pts ≈ −8.5¢/contract (side-1 −15.8pts; toxic window 6-24h
pre-start, −10.4pts); micro markouts CLEAN (+0.35¢@5m..+0.86¢@2h) — the
market knew resolutions, it did not pick off ticks. Config gap: flat 5¢
side-1 floor (logit+0.6 keeps $133 at +92%, refuses $1,500 at −36.6%) and
missing skip-NO<45% (pocket −14.9%). Blame: config ≈60-70%, model ≈25%
(in-window favorite calibration −3.9pp, n.s.), drift/fees/noise exonerated.
Market data remains diagnostic only — this section feeds no solve.

## §8 Research log

Preregistration-vs-outcome: 20 numeric predictions plotted
(`page_prereg_scatter.json`, quotes verified verbatim at build); band-misses
and fired falsifiers recorded at the same resolution as holds (S3's
anti-synergy falsifier and patch-fade's kill are the system working).
Session timeline in `page_timeline.json`. **The adversary's report publishes
verbatim** — see `adversary_report.md`; his bottom line: *"v6 stands; nothing
is promotable; cold-start handling is the one lead that has earned a test on
new data."*

## §9 Slate

v6 snapshot prices only (`page_slate.json`, frozen benpom-v6-2026-07-22 via
`trading_model/predict.py`). **No v8 column — no candidate was promoted.**
The flag rule stays: a future promoted candidate gets a column plus a
divergence flag at |candidate − v6| ≥ 5pts.

## §10 Tripwires & ledger (machine-readable: `stats/ledger_v8_updates.json`)

- **Do-not-retest additions** (kill evidence attached in the JSON): H1
  censoring premise; round-level power play; H2 connectivity; H4 dispersion
  premise+links; learned event-class weights; patch fade (γ≤0.7);
  performance-form definitions; lineup-conditioned EWC down-weight;
  calendar-noise state-space; the S3 anti-synergy fitting shape.
- **Re-openable:** the 27 rejected→unresolved ledger entries (listed in the
  JSON with Δ, n and MDE-at-test). Power-annotation rule applies.
- **Tripwires** (full thresholds/windows in the JSON): frozen-5d cold-start
  test at ≥40 fresh cold rows; 3e/event-class fade re-test only at a ≥300-
  series expansion; live favorite-calibration alarm at ±5pp on n≥100; PRX/NRG
  bias alarm at −12pp rolling-60; bucket claims must quote their floor
  (advisory-only under n=200); config review if the unchanged live config
  loses ≥$150 over the next 28 settled events.
- **What data next:** prospective logger accumulation; the deferred 194
  prefranchise regional events; 2026 S2/Champions as they settle.

---
*v8 Lab · program run 2026-07-28 · CRN referee throughout · v6 remains the
champion and the trading_model snapshot is unchanged · adversary-amended
verdicts are final.*
